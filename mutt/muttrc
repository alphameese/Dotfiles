set ssl_starttls=yes
set ssl_force_tls=yes

set imap_user	= 'andrewfsas@gmail.com'

set imap_pass	= 'suyf kwxx kghd eflh'

set from	= 'andrewfsas@gmail.com'

set realname	= 'Andrew Sas'

set folder	= imaps://imap.gmail.com

set spoolfile	= imaps://imap.gmail.com/INBOX

set postponed	= "imaps://imap.gmail.com/[Gmail]/Drafts"

set smtp_url	= "smtp://andrewfsas@smtp.gmail.com:587/"

set move = no

set header_cache = "~/dotfiles/.mutt/cache/headers"

set message_cachedir = "~/dotfiles/.mutt/cache/bodies"

set certificate_file = ~/dotfiles/.mutt/certificates

unset imap_passive

set imap_keepalive = 300

set mail_check = 120

set sort = reverse-date-received

set sidebar_visible = yes


mailboxes +INBOX
